{
  "name": "zkdeal-prover-agent",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "queue-pull sidecar: leases proof jobs from the shared prove queue and forwards them to the local CUDA prover",
  "scripts": {
    "build": "tsc --noEmit",
    "start": "tsx src/agent.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "viem": "^2.55.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
